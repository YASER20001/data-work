# =============================================================
# SafeAssist / RIFD — Environment Configuration
# Place this file at the project root (same level as app.py)
# =============================================================

# ─────────────────────────────────────────────
# REQUIRED — will crash on startup without this
# ─────────────────────────────────────────────

# Your Google Gemini API key (from https://aistudio.google.com)
GEMINI_API_KEY="AIzaSyAmVd9oIh0cnu1nR8NmixpyH6sHHud3IEU"

# ─────────────────────────────────────────────
# GEMINI MODEL — optional, has a default
# ─────────────────────────────────────────────

# Which Gemini model to use for all LLM calls.
# Default if omitted: gemini-2.5-flash
GEMINI_MODEL=gemini-2.5-flash

# ─────────────────────────────────────────────
# GOOGLE CLOUD — needed for STT and TTS (voice mode)
# ─────────────────────────────────────────────

# Absolute path to your Google Cloud service account JSON file.
# Required for: Speech-to-Text (STT) and Text-to-Speech (TTS).
# If you don't use voice mode, you can leave this blank.
SAFEASSIST_GOOGLE_CREDENTIALS_PATH=/absolute/path/to/your/google-credentials.json

# ─────────────────────────────────────────────
# HUME AI — needed for emotional tone analysis in voice mode
# ─────────────────────────────────────────────

# API key from https://hume.ai
# Used to detect emotional tone from audio (prosody analysis).
# If left blank, tone will show as "Tone: —" but app still works.
HUME_API_KEY=your_hume_api_key_here

# ─────────────────────────────────────────────
# FEATURE FLAGS — all optional
# ─────────────────────────────────────────────

# Enable voice mode (TTS audio output). Default: false
# Set to true only if you have SAFEASSIST_GOOGLE_CREDENTIALS_PATH set.
VOICE_MODE=false

# Enable extra debug output from the router agent. Default: 0
ROUTER_DEBUG=0

# Enable hard-risk keyword tripwire (bypasses LLM for immediate danger words).
# Strongly recommended to keep ON (1). Default: 1
ROUTER_ENABLE_DANGER_FLAG=1
